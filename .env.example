# App
ENV=dev

# DB (lokal via docker-compose)
DB_HOST=db
DB_PORT=5432
DB_NAME=verdiant
DB_USER=verdiant
DB_PASSWORD=verdiant

# Async URL (von backend/.env genutzt)
DATABASE_URL=postgresql+asyncpg://verdiant:verdiant@db:5432/verdiant

# CORS (optional, kommasepariert)
CORS_ORIGINS=http://localhost:5173
